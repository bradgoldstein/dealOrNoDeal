from card import Card
import random

def shuffle (deck):

    random.shuffle(deck)
    #print deck

#deck=[ Card("spades", 2), Card("hearts", 4), Card("hearts", 1), Card("clubs", 12), Card("spades", 3) ]
#print shuffle(deck)
